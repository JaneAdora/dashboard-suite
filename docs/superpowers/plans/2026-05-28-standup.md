---
type: plan
title: standup-glance-panel-plan
status: ready
date: 2026-05-28
spec: ../specs/2026-05-28-standup-design.md
---

# `standup` glance panel Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a `standup` glance panel that renders a compact today-scoreboard of commits, Claude Code sessions, and calendar meetings, plus a one-line yesterday comparison.

**Architecture:** Single new file `src/panels/standup.rs`. Two background threads (git scan + session walk) ship results over a shared `mpsc<Msg>` channel. `CalCore` is reused (via its public `days: Vec<DayGroup>`) for meetings; one minimal addition to `src/cal/mod.rs` (a persistent `last_fetch_error` flag) lets standup distinguish "no meetings today" from "calendar unavailable". Three pure helpers (`summarize_commits`, `count_sessions`, `summarize_meetings`) are unit-tested; the render function is verified manually.

**Tech Stack:** Rust 2021, `ratatui` 0.29, `jiff` 0.2 (already a glance dep) for local-time math, `std::thread` + `mpsc` for I/O isolation. No new dependencies.

**Revised 2026-05-28 (post-review):** DST-safe day boundaries, in-progress-meeting handling with a strictly-future "next", `commits.rs` repo helpers reused via `pub(crate)` instead of copied, and a calendar-unavailable row backed by a new `CalCore::last_fetch_error` flag.

---

## File structure

- **Create:** `~/projects/glance/src/panels/standup.rs` (panel; struct, threads, render, tests; ~350 lines incl. tests)
- **Modify:** `~/projects/glance/src/panels/mod.rs` (add module decl, `DEFAULT_ORDER` entry, `ALL_PANELS` entry, `build_panel` arm)
- **Modify:** `~/projects/dashboard-suite/suite.toml` (add `[panel.standup]` entry)

`theme.rs` is read-only. `commits.rs` gets a 2-line visibility bump (`project_roots`/`find_repos` -> `pub(crate)`) so standup imports them rather than copying. `cal/mod.rs` gains a `last_fetch_error` field. All other logic is new code in `standup.rs`.

---

### Task 1: Skeleton panel + registration

Create a minimal `standup.rs` that compiles, renders `standup loading…`, and is registered in the panel registry. Unblocks every subsequent task.

**Files:**
- Create: `~/projects/glance/src/panels/standup.rs`
- Modify: `~/projects/glance/src/panels/mod.rs`

- [ ] **Step 1.1: Write the failing test**

Append to `~/projects/glance/src/panels/mod.rs`:

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn build_panel_knows_standup() {
        let p = build_panel("standup");
        assert!(p.is_some(), "build_panel should return Some for \"standup\"");
        assert_eq!(p.unwrap().name(), "standup");
    }

    #[test]
    fn standup_is_in_all_panels() {
        assert!(ALL_PANELS.iter().any(|n| *n == "standup"));
    }
}
```

- [ ] **Step 1.2: Run test to verify it fails**

```
cd ~/projects/glance && cargo test --lib panels::tests
```

Expected: compile error or `FAILED` because `standup` is unknown and not in `ALL_PANELS`.

- [ ] **Step 1.3: Create the minimal panel**

Write `~/projects/glance/src/panels/standup.rs`:

```rust
//! `standup` -- today-scoreboard glance panel.
//!
//! Synthesizes today's git commits, Claude Code sessions, and calendar meetings
//! into a single compact tile. Spec: docs/superpowers/specs/2026-05-28-standup-design.md
use crate::panels::Panel;
use crate::theme;
use ratatui::layout::Rect;
use ratatui::text::{Line, Span};
use ratatui::widgets::Paragraph;
use ratatui::Frame;

pub struct StandupPanel {}

impl StandupPanel {
    pub fn new() -> Self { Self {} }
}

impl Panel for StandupPanel {
    fn name(&self) -> &str { "standup" }
    fn refresh_ms(&self) -> u64 { 60_000 }
    fn tick(&mut self) {}
    fn render(&self, f: &mut Frame, area: Rect) {
        let title = Line::from(Span::styled(" standup ", theme::pane_header()));
        let body = Line::from(Span::styled("loading...", theme::dim()));
        f.render_widget(Paragraph::new(vec![title, body]), area);
    }
}
```

- [ ] **Step 1.4: Register the panel**

Edit `~/projects/glance/src/panels/mod.rs`:

1. Add `pub mod standup;` near sibling panel modules.
2. In `DEFAULT_ORDER`, append `"standup"` after `"tasks"`.
3. In `ALL_PANELS`, append `"standup"` after `"tasks"`.
4. In `build_panel`, add an arm after the `"tasks"` arm:

```rust
"standup" => Box::new(standup::StandupPanel::new()),
```

- [ ] **Step 1.5: Run test to verify it passes**

```
cd ~/projects/glance && cargo test --lib panels::tests
```

Expected: both tests pass.

- [ ] **Step 1.6: Smoke test in glance**

```
cd ~/projects/glance && cargo build --release && ./target/release/glance
```

Expected: glance launches; switching to standup shows ` standup ` header and dim `loading...` line.

- [ ] **Step 1.7: Commit**

```
cd ~/projects/glance && git add src/panels/standup.rs src/panels/mod.rs && git commit -m "standup: skeleton panel + registration"
```

---

### Task 2: Day boundary helpers + tests

Local-tz "today midnight" and "yesterday midnight" via `jiff`. All three sources use the resulting `SystemTime` boundaries.

**Files:**
- Modify: `~/projects/glance/src/panels/standup.rs`

- [ ] **Step 2.1: Write the failing tests**

Append to `~/projects/glance/src/panels/standup.rs`:

```rust
use std::time::SystemTime;

/// Returns `(today_midnight, yesterday_midnight)` as `SystemTime`s in the
/// local timezone. Seconds-precision.
fn day_boundaries(now: jiff::Zoned) -> (SystemTime, SystemTime) {
    let _ = now;
    unimplemented!()
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn day_boundaries_today_is_local_midnight() {
        let now: jiff::Zoned = "2026-05-28T14:32:00-05:00[America/Chicago]"
            .parse()
            .unwrap();
        let (today, yesterday) = day_boundaries(now);
        let expected_today = std::time::UNIX_EPOCH
            + std::time::Duration::from_secs(1779944400);
        assert_eq!(today, expected_today);
        assert_eq!(today.duration_since(yesterday).unwrap().as_secs(), 86_400);
    }

    #[test]
    fn day_boundaries_at_local_midnight_returns_that_midnight() {
        let now: jiff::Zoned = "2026-05-28T00:00:00-05:00[America/Chicago]"
            .parse()
            .unwrap();
        let (today, _) = day_boundaries(now);
        let expected_today = std::time::UNIX_EPOCH
            + std::time::Duration::from_secs(1779944400);
        assert_eq!(today, expected_today);
    }
}
```

`1779944400` is the Unix-seconds value of `2026-05-28T00:00:00-05:00`. Verify with `date -d '2026-05-28T00:00:00-05:00' +%s`. The `today - yesterday == 86_400` assertion holds because May 28 is not a DST-transition day; the implementation derives each boundary from its civil date, so DST days stay correct even though their span is not 24h.

- [ ] **Step 2.2: Run test to verify it fails**

```
cd ~/projects/glance && cargo test --lib panels::standup::tests
```

Expected: panic in `unimplemented!()`.

- [ ] **Step 2.3: Implement `day_boundaries`**

Replace the body:

```rust
use jiff::ToSpan;

/// Local-tz civil midnight for `date`, as a `SystemTime`.
fn civil_midnight_systemtime(date: jiff::civil::Date, tz: &jiff::tz::TimeZone) -> SystemTime {
    let secs = date
        .at(0, 0, 0, 0)
        .to_zoned(tz.clone())
        .expect("local midnight is always representable")
        .timestamp()
        .as_second();
    std::time::UNIX_EPOCH + std::time::Duration::from_secs(secs as u64)
}

fn day_boundaries(now: jiff::Zoned) -> (SystemTime, SystemTime) {
    let tz = now.time_zone().clone();
    let today_date = now.date();
    let yesterday_date = today_date.checked_sub(1.day()).unwrap_or(today_date);
    // Derive each midnight from its civil date so DST-transition days stay
    // correct (a flat -86_400 s would drift an hour twice a year).
    (
        civil_midnight_systemtime(today_date, &tz),
        civil_midnight_systemtime(yesterday_date, &tz),
    )
}
```

- [ ] **Step 2.4: Run tests to verify they pass**

```
cd ~/projects/glance && cargo test --lib panels::standup::tests
```

Expected: both `day_boundaries_*` tests pass.

- [ ] **Step 2.5: Commit**

```
cd ~/projects/glance && git add src/panels/standup.rs && git commit -m "standup: local-tz day boundary helper + tests"
```

---

### Task 3: `summarize_commits` pure helper + tests

Aggregates pre-parsed `git log --format=%cI|%H|%s` rows across multiple repos into a `CommitsSnapshot`. No I/O.

**Files:**
- Modify: `~/projects/glance/src/panels/standup.rs`

- [ ] **Step 3.1: Write the failing tests**

Add to `standup.rs` above the `#[cfg(test)]` block:

```rust
#[derive(Default, Clone, Debug)]
pub struct CommitsSnapshot {
    pub total: u32,
    pub repos_touched: u32,
    /// Most recent committer time, unix seconds.
    pub last_at: Option<i64>,
}

/// Aggregates `(repo_label, %cI, %H, %s)` rows into a snapshot.
fn summarize_commits(lines: &[(String, String, String, String)]) -> CommitsSnapshot {
    let _ = lines;
    unimplemented!()
}
```

Append inside `mod tests`:

```rust
fn cm(repo: &str, ci: &str, sha: &str, subj: &str) -> (String, String, String, String) {
    (repo.into(), ci.into(), sha.into(), subj.into())
}

#[test]
fn summarize_commits_empty_is_zero() {
    let s = summarize_commits(&[]);
    assert_eq!(s.total, 0);
    assert_eq!(s.repos_touched, 0);
    assert_eq!(s.last_at, None);
}

#[test]
fn summarize_commits_counts_across_repos() {
    let rows = vec![
        cm("/p/a", "2026-05-28T09:00:00-05:00", "aaa", "first"),
        cm("/p/a", "2026-05-28T11:30:00-05:00", "bbb", "second"),
        cm("/p/b", "2026-05-28T14:14:00-05:00", "ccc", "third"),
    ];
    let s = summarize_commits(&rows);
    assert_eq!(s.total, 3);
    assert_eq!(s.repos_touched, 2);
    let expected_last: i64 = "2026-05-28T14:14:00-05:00"
        .parse::<jiff::Timestamp>().unwrap().as_second();
    assert_eq!(s.last_at, Some(expected_last));
}

#[test]
fn summarize_commits_skips_malformed_time() {
    let rows = vec![
        cm("/p/a", "not-a-date", "aaa", "first"),
        cm("/p/a", "2026-05-28T11:30:00-05:00", "bbb", "second"),
    ];
    let s = summarize_commits(&rows);
    assert_eq!(s.total, 2);
    assert_eq!(s.repos_touched, 1);
    let expected_last: i64 = "2026-05-28T11:30:00-05:00"
        .parse::<jiff::Timestamp>().unwrap().as_second();
    assert_eq!(s.last_at, Some(expected_last));
}
```

- [ ] **Step 3.2: Run tests to verify they fail**

```
cd ~/projects/glance && cargo test --lib panels::standup::tests::summarize_commits
```

Expected: panics in `unimplemented!()`.

- [ ] **Step 3.3: Implement `summarize_commits`**

Replace the body:

```rust
fn summarize_commits(lines: &[(String, String, String, String)]) -> CommitsSnapshot {
    let mut repos: std::collections::HashSet<&str> = std::collections::HashSet::new();
    let mut last_at: Option<i64> = None;
    for (repo, ci, _sha, _subj) in lines {
        repos.insert(repo.as_str());
        if let Ok(ts) = ci.parse::<jiff::Timestamp>() {
            let secs = ts.as_second();
            last_at = Some(last_at.map_or(secs, |prev| prev.max(secs)));
        }
    }
    CommitsSnapshot {
        total: lines.len() as u32,
        repos_touched: repos.len() as u32,
        last_at,
    }
}
```

- [ ] **Step 3.4: Run tests to verify they pass**

```
cd ~/projects/glance && cargo test --lib panels::standup::tests::summarize_commits
```

Expected: all three tests pass.

- [ ] **Step 3.5: Commit**

```
cd ~/projects/glance && git add src/panels/standup.rs && git commit -m "standup: summarize_commits helper + tests"
```

---

### Task 4: `count_sessions` pure helper + tests

Pure filter over `&[SystemTime]` and a `[start, end)` half-open range.

**Files:**
- Modify: `~/projects/glance/src/panels/standup.rs`

- [ ] **Step 4.1: Write the failing tests**

Add above `#[cfg(test)]`:

```rust
#[derive(Default, Clone, Debug)]
pub struct SessionsSnapshot {
    pub count: u32,
    /// Most recent mtime within the range, unix seconds.
    pub last_at: Option<i64>,
}

fn count_sessions(
    mtimes: &[SystemTime],
    start: SystemTime,
    end: SystemTime,
) -> SessionsSnapshot {
    let _ = (mtimes, start, end);
    unimplemented!()
}
```

Append in `mod tests`:

```rust
fn t(secs: u64) -> SystemTime {
    std::time::UNIX_EPOCH + std::time::Duration::from_secs(secs)
}

#[test]
fn count_sessions_empty_is_zero() {
    let s = count_sessions(&[], t(0), t(1));
    assert_eq!(s.count, 0);
    assert_eq!(s.last_at, None);
}

#[test]
fn count_sessions_filters_to_range() {
    let mtimes = vec![t(100), t(200), t(300), t(400)];
    let s = count_sessions(&mtimes, t(150), t(350));
    assert_eq!(s.count, 2);
    assert_eq!(s.last_at, Some(300));
}

#[test]
fn count_sessions_end_is_exclusive() {
    let mtimes = vec![t(200)];
    let s = count_sessions(&mtimes, t(100), t(200));
    assert_eq!(s.count, 0);
}

#[test]
fn count_sessions_start_is_inclusive() {
    let mtimes = vec![t(200)];
    let s = count_sessions(&mtimes, t(200), t(300));
    assert_eq!(s.count, 1);
    assert_eq!(s.last_at, Some(200));
}
```

- [ ] **Step 4.2: Run tests to verify they fail**

```
cd ~/projects/glance && cargo test --lib panels::standup::tests::count_sessions
```

Expected: panics.

- [ ] **Step 4.3: Implement `count_sessions`**

```rust
fn count_sessions(
    mtimes: &[SystemTime],
    start: SystemTime,
    end: SystemTime,
) -> SessionsSnapshot {
    let mut count: u32 = 0;
    let mut last_at: Option<i64> = None;
    for m in mtimes {
        if *m >= start && *m < end {
            count += 1;
            let secs = m
                .duration_since(std::time::UNIX_EPOCH)
                .map(|d| d.as_secs() as i64)
                .unwrap_or(0);
            last_at = Some(last_at.map_or(secs, |prev| prev.max(secs)));
        }
    }
    SessionsSnapshot { count, last_at }
}
```

- [ ] **Step 4.4: Run tests to verify they pass**

```
cd ~/projects/glance && cargo test --lib panels::standup::tests::count_sessions
```

Expected: all four tests pass.

- [ ] **Step 4.5: Commit**

```
cd ~/projects/glance && git add src/panels/standup.rs && git commit -m "standup: count_sessions helper + tests"
```

---

### Task 5: `summarize_meetings` pure helper + tests

Splits `&[Event]` into done/upcoming/next by comparing to `now`. Uses existing `Event` API.

**Files:**
- Modify: `~/projects/glance/src/panels/standup.rs`

- [ ] **Step 5.1: Write the failing tests**

Add above `#[cfg(test)]`:

```rust
use crate::cal::event::Event;

#[derive(Clone, Debug)]
pub struct EventLite {
    pub start_secs: i64,
    pub title: String,
}

#[derive(Default, Clone, Debug)]
pub struct MeetingsSnapshot {
    pub done: u32,
    /// Not-yet-finished meetings: in-progress + strictly future. `done + upcoming`
    /// equals the total counted meetings for the day.
    pub upcoming: u32,
    pub next: Option<EventLite>,
}

/// Splits a day's events into done/upcoming relative to `now`, and selects
/// the next upcoming event by start time. Skips all-day, declined, and
/// events whose timestamps fail to parse.
fn summarize_meetings(events: &[Event], now: jiff::Timestamp) -> MeetingsSnapshot {
    let _ = (events, now);
    unimplemented!()
}
```

Append in `mod tests`:

```rust
use crate::cal::event::{Attendee, ResponseStatus};

fn make_event(start: &str, end: &str, title: &str) -> Event {
    Event {
        id: title.into(),
        summary: title.into(),
        description: String::new(),
        location: String::new(),
        start: start.into(),
        end: end.into(),
        all_day: false,
        status: "confirmed".into(),
        html_link: String::new(),
        hangout_link: String::new(),
        meet_url: String::new(),
        attendees: vec![Attendee {
            email: "jane@repcap.com".into(),
            name: "Jane".into(),
            response_status: ResponseStatus::Accepted,
            is_self: true,
            organizer: false,
        }],
        is_recurring: false,
        recurring_event_id: String::new(),
        calendar_id: "primary".into(),
    }
}

#[test]
fn summarize_meetings_empty_is_zero() {
    let now: jiff::Timestamp = "2026-05-28T14:00:00-05:00".parse().unwrap();
    let s = summarize_meetings(&[], now);
    assert_eq!(s.done, 0);
    assert_eq!(s.upcoming, 0);
    assert!(s.next.is_none());
}

#[test]
fn summarize_meetings_splits_by_now() {
    let now: jiff::Timestamp = "2026-05-28T14:00:00-05:00".parse().unwrap();
    let events = vec![
        make_event("2026-05-28T09:00:00-05:00", "2026-05-28T09:30:00-05:00", "morning"),
        make_event("2026-05-28T13:00:00-05:00", "2026-05-28T13:30:00-05:00", "lunch"),
        make_event("2026-05-28T16:00:00-05:00", "2026-05-28T16:30:00-05:00", "thelma sync"),
        make_event("2026-05-28T17:00:00-05:00", "2026-05-28T17:30:00-05:00", "wrap"),
    ];
    let s = summarize_meetings(&events, now);
    assert_eq!(s.done, 2);
    assert_eq!(s.upcoming, 2);
    let next = s.next.expect("next event present");
    assert_eq!(next.title, "thelma sync");
    let expected_start: i64 = "2026-05-28T16:00:00-05:00"
        .parse::<jiff::Timestamp>().unwrap().as_second();
    assert_eq!(next.start_secs, expected_start);
}

#[test]
fn summarize_meetings_skips_declined() {
    let now: jiff::Timestamp = "2026-05-28T14:00:00-05:00".parse().unwrap();
    let mut declined = make_event(
        "2026-05-28T16:00:00-05:00",
        "2026-05-28T16:30:00-05:00",
        "declined",
    );
    declined.attendees[0].response_status = ResponseStatus::Declined;
    let kept = make_event(
        "2026-05-28T17:00:00-05:00",
        "2026-05-28T17:30:00-05:00",
        "kept",
    );
    let s = summarize_meetings(&[declined, kept], now);
    assert_eq!(s.done, 0);
    assert_eq!(s.upcoming, 1);
    assert_eq!(s.next.unwrap().title, "kept");
}

#[test]
fn summarize_meetings_skips_all_day() {
    let now: jiff::Timestamp = "2026-05-28T14:00:00-05:00".parse().unwrap();
    let mut all_day = make_event("2026-05-28", "2026-05-29", "OOO");
    all_day.all_day = true;
    let timed = make_event(
        "2026-05-28T16:00:00-05:00",
        "2026-05-28T16:30:00-05:00",
        "real",
    );
    let s = summarize_meetings(&[all_day, timed], now);
    assert_eq!(s.upcoming, 1);
    assert_eq!(s.next.unwrap().title, "real");
}

#[test]
fn summarize_meetings_in_progress_counts_but_is_not_next() {
    let now: jiff::Timestamp = "2026-05-28T14:00:00-05:00".parse().unwrap();
    let in_progress = make_event(
        "2026-05-28T13:45:00-05:00",
        "2026-05-28T14:30:00-05:00",
        "in progress",
    );
    let later = make_event(
        "2026-05-28T16:00:00-05:00",
        "2026-05-28T16:30:00-05:00",
        "later",
    );
    let s = summarize_meetings(&[in_progress, later], now);
    assert_eq!(s.done, 0);
    assert_eq!(s.upcoming, 2); // both not-yet-finished
    assert_eq!(s.next.unwrap().title, "later"); // in-progress skipped as "next"
}
```

- [ ] **Step 5.2: Run tests to verify they fail**

```
cd ~/projects/glance && cargo test --lib panels::standup::tests::summarize_meetings
```

Expected: panics.

- [ ] **Step 5.3: Implement `summarize_meetings`**

```rust
fn summarize_meetings(events: &[Event], now: jiff::Timestamp) -> MeetingsSnapshot {
    let mut done: u32 = 0;
    let mut upcoming: u32 = 0;
    let mut next_pair: Option<(i64, String)> = None;

    for ev in events {
        if ev.all_day || ev.is_declined() {
            continue;
        }
        let Some(start) = ev.start_ts() else { continue };
        if ev.is_past(&now) {
            done += 1;
            continue;
        }
        upcoming += 1; // counts a meeting in progress right now, too
        let start_secs = start.as_second();
        if start_secs > now.as_second() {
            // Only a strictly-future event is eligible as "next", so the tile
            // never prints a clock time that has already passed.
            let take = match &next_pair {
                None => true,
                Some((prev_secs, _)) => start_secs < *prev_secs,
            };
            if take {
                next_pair = Some((start_secs, ev.summary.clone()));
            }
        }
    }

    MeetingsSnapshot {
        done,
        upcoming,
        next: next_pair.map(|(start_secs, title)| EventLite { start_secs, title }),
    }
}
```

- [ ] **Step 5.4: Run tests to verify they pass**

```
cd ~/projects/glance && cargo test --lib panels::standup::tests::summarize_meetings
```

Expected: all four tests pass.

- [ ] **Step 5.5: Commit**

```
cd ~/projects/glance && git add src/panels/standup.rs && git commit -m "standup: summarize_meetings helper + tests"
```

---

### Task 6: Wire git scan thread

Background thread walks `project_roots()`, calls `git log` per repo with explicit `--since`/`--until` bounds, and sends today + yesterday snapshots over a shared `mpsc<Msg>`.

**Files:**
- Modify: `~/projects/glance/src/panels/standup.rs`

- [ ] **Step 6.1: Make `commits.rs` helpers reusable, then add struct, channel, and scan code**

First, in `~/projects/glance/src/panels/commits.rs`, change `fn project_roots(` to `pub(crate) fn project_roots(` and `fn find_repos(` to `pub(crate) fn find_repos(`. No other change to that file.

Then add imports near the top of `standup.rs`:

```rust
use std::path::{Path, PathBuf};
use std::process::Command;
use std::sync::mpsc;
use std::thread;
use std::time::{Duration, Instant};
```

Add helpers:

```rust
// project_roots + find_repos are reused from commits.rs (made pub(crate) below).
use crate::panels::commits::{find_repos, project_roots};

fn git_log_rows(
    repo: &Path,
    since: &str,
    until: &str,
) -> Vec<(String, String, String, String)> {
    let res = Command::new("git")
        .arg("-C").arg(repo)
        .args([
            "log",
            &format!("--since={since}"),
            &format!("--until={until}"),
            "--format=%cI|%H|%s",
            "--all",
            "--no-merges",
        ])
        .output();
    let stdout = match res {
        Ok(o) if o.status.success() => o.stdout,
        _ => return Vec::new(),
    };
    let repo_label = repo.to_string_lossy().to_string();
    let mut out = Vec::new();
    for line in String::from_utf8_lossy(&stdout).lines() {
        let mut parts = line.splitn(3, '|');
        let (Some(ci), Some(sha), Some(subj)) = (parts.next(), parts.next(), parts.next())
        else { continue };
        out.push((repo_label.clone(), ci.to_string(), sha.to_string(), subj.to_string()));
    }
    out
}

fn format_iso(t: SystemTime) -> String {
    let secs = t.duration_since(std::time::UNIX_EPOCH)
        .map(|d| d.as_secs() as i64).unwrap_or(0);
    jiff::Timestamp::from_second(secs)
        .map(|ts| ts.to_string())
        .unwrap_or_else(|_| "1970-01-01T00:00:00Z".to_string())
}

fn scan_commits(
    today_start: SystemTime,
    yesterday_start: SystemTime,
) -> (CommitsSnapshot, CommitsSnapshot) {
    let today_iso = format_iso(today_start);
    let yesterday_iso = format_iso(yesterday_start);
    let now_iso = format_iso(SystemTime::now());

    let mut today_rows = Vec::new();
    let mut yesterday_rows = Vec::new();
    for root in project_roots() {
        for repo in find_repos(&root) {
            today_rows.extend(git_log_rows(&repo, &today_iso, &now_iso));
            yesterday_rows.extend(git_log_rows(&repo, &yesterday_iso, &today_iso));
        }
    }
    (summarize_commits(&today_rows), summarize_commits(&yesterday_rows))
}
```

Replace the placeholder `pub struct StandupPanel {}` and its impl with:

```rust
enum Msg {
    Commits { today: CommitsSnapshot, yesterday: CommitsSnapshot },
    Sessions { today: SessionsSnapshot, yesterday: SessionsSnapshot },
}

#[derive(Default, Clone)]
struct Snapshot {
    commits: CommitsSnapshot,
    sessions: SessionsSnapshot,
    meetings: MeetingsSnapshot,
}

pub struct StandupPanel {
    today: Snapshot,
    yesterday: Snapshot,
    last_git_scan: Option<Instant>,
    last_session_scan: Option<Instant>,
    rx: mpsc::Receiver<Msg>,
    tx: mpsc::Sender<Msg>,
    loading_git: bool,
    loading_sessions: bool,
    last_date_seen: Option<jiff::civil::Date>,
}

impl StandupPanel {
    pub fn new() -> Self {
        let (tx, rx) = mpsc::channel();
        Self {
            today: Snapshot::default(),
            yesterday: Snapshot::default(),
            last_git_scan: None,
            last_session_scan: None,
            rx,
            tx,
            loading_git: false,
            loading_sessions: false,
            last_date_seen: None,
        }
    }

    fn kick_git_scan(&mut self) {
        let tx = self.tx.clone();
        let now = jiff::Zoned::now();
        let (today_start, yesterday_start) = day_boundaries(now);
        self.loading_git = true;
        self.last_git_scan = Some(Instant::now());
        thread::spawn(move || {
            let (today, yesterday) = scan_commits(today_start, yesterday_start);
            let _ = tx.send(Msg::Commits { today, yesterday });
        });
    }
}

impl Panel for StandupPanel {
    fn name(&self) -> &str { "standup" }
    fn refresh_ms(&self) -> u64 { 60_000 }

    fn tick(&mut self) {
        while let Ok(msg) = self.rx.try_recv() {
            match msg {
                Msg::Commits { today, yesterday } => {
                    self.today.commits = today;
                    self.yesterday.commits = yesterday;
                    self.loading_git = false;
                }
                Msg::Sessions { today, yesterday } => {
                    self.today.sessions = today;
                    self.yesterday.sessions = yesterday;
                    self.loading_sessions = false;
                }
            }
        }
        let stale_git = self.last_git_scan
            .map(|t| t.elapsed() > Duration::from_secs(300))
            .unwrap_or(true);
        if stale_git && !self.loading_git {
            self.kick_git_scan();
        }
    }

    fn render(&self, f: &mut Frame, area: Rect) {
        let title = Line::from(Span::styled(" standup ", theme::pane_header()));
        let body = Line::from(Span::styled(
            format!(
                "today: {} commits / {} sessions / {} meetings (loading)",
                self.today.commits.total,
                self.today.sessions.count,
                self.today.meetings.done + self.today.meetings.upcoming
            ),
            theme::dim(),
        ));
        f.render_widget(Paragraph::new(vec![title, body]), area);
    }
}
```

- [ ] **Step 6.2: Build to confirm it compiles**

```
cd ~/projects/glance && cargo build --release
```

Expected: clean build (warnings about unused-yet `sessions`/`meetings` are OK).

- [ ] **Step 6.3: Run all tests**

```
cd ~/projects/glance && cargo test --lib panels::standup::tests
```

Expected: every previously-passing test still passes.

- [ ] **Step 6.4: Manual smoke**

```
./target/release/glance
```

Switch to standup; commits count should populate within ~30s.

- [ ] **Step 6.5: Commit**

```
cd ~/projects/glance && git add src/panels/standup.rs src/panels/commits.rs && git commit -m "standup: background git scan thread (reuse commits.rs repo helpers)"
```

---

### Task 7: Wire session scan thread

Walks `~/.claude/projects/<slug>/*.jsonl`, collects mtimes, runs `count_sessions` twice (today + yesterday).

**Files:**
- Modify: `~/projects/glance/src/panels/standup.rs`

- [ ] **Step 7.1: Add session walker + kick**

Add helpers:

```rust
fn claude_projects_dir() -> Option<PathBuf> {
    dirs::home_dir().map(|h| h.join(".claude").join("projects"))
}

fn collect_session_mtimes() -> Vec<SystemTime> {
    let Some(root) = claude_projects_dir() else { return Vec::new() };
    let outer = match std::fs::read_dir(&root) {
        Ok(d) => d,
        Err(_) => return Vec::new(),
    };
    let mut out = Vec::new();
    for project in outer.flatten() {
        let pp = project.path();
        if !pp.is_dir() { continue; }
        let inner = match std::fs::read_dir(&pp) {
            Ok(d) => d,
            Err(_) => continue,
        };
        for f in inner.flatten() {
            let fp = f.path();
            if fp.extension().and_then(|e| e.to_str()) != Some("jsonl") {
                continue;
            }
            if let Ok(meta) = f.metadata() {
                if let Ok(mtime) = meta.modified() {
                    out.push(mtime);
                }
            }
        }
    }
    out
}

fn scan_sessions(
    today_start: SystemTime,
    yesterday_start: SystemTime,
) -> (SessionsSnapshot, SessionsSnapshot) {
    let mtimes = collect_session_mtimes();
    let now = SystemTime::now();
    let today = count_sessions(&mtimes, today_start, now);
    let yesterday = count_sessions(&mtimes, yesterday_start, today_start);
    (today, yesterday)
}
```

Add `kick_session_scan` to `impl StandupPanel`:

```rust
fn kick_session_scan(&mut self) {
    let tx = self.tx.clone();
    let now = jiff::Zoned::now();
    let (today_start, yesterday_start) = day_boundaries(now);
    self.loading_sessions = true;
    self.last_session_scan = Some(Instant::now());
    thread::spawn(move || {
        let (today, yesterday) = scan_sessions(today_start, yesterday_start);
        let _ = tx.send(Msg::Sessions { today, yesterday });
    });
}
```

Extend `tick` to kick the session scan after the git stale-check:

```rust
let stale_sessions = self.last_session_scan
    .map(|t| t.elapsed() > Duration::from_secs(300))
    .unwrap_or(true);
if stale_sessions && !self.loading_sessions {
    self.kick_session_scan();
}
```

- [ ] **Step 7.2: Build to confirm it compiles**

```
cd ~/projects/glance && cargo build --release
```

Expected: clean build.

- [ ] **Step 7.3: Run all tests**

```
cd ~/projects/glance && cargo test --lib panels::standup
```

Expected: all pass.

- [ ] **Step 7.4: Manual smoke**

```
./target/release/glance
```

Switch to standup. Commits + sessions counts both populate.

- [ ] **Step 7.5: Commit**

```
cd ~/projects/glance && git add src/panels/standup.rs && git commit -m "standup: background session scan thread"
```

---

### Task 8: Wire CalCore into tick

Owns a `CalCore`, calls `tick()` each iteration, refreshes `today.meetings` + `yesterday.meetings` from `core.days`. Handles date rollover.

**Files:**
- Modify: `~/projects/glance/src/panels/standup.rs`

- [ ] **Step 8.1: Add CalCore + meetings refresh**

Add the import (`jiff::ToSpan` is already in scope from Task 2):

```rust
use crate::cal::CalCore;
```

Then edit `~/projects/glance/src/cal/mod.rs` to expose a persistent fetch-error
signal (the existing `last_toast` self-clears after 3s, so it cannot drive a
steady row):

1. Add a field to `struct CalCore`: `pub last_fetch_error: Option<String>,`
2. In `CalCore::new`, initialize it in the `Self { ... }` literal: `last_fetch_error: None,`
3. In `CalCore::tick`, in the `Ok(Ok(result))` arm set `self.last_fetch_error = None;`
   just before `self.apply_fetch(result);`; in the `Ok(Err(e))` arm set
   `self.last_fetch_error = Some(format!("{e}"));` alongside the existing toast.

Add a `cal` field:

```rust
pub struct StandupPanel {
    today: Snapshot,
    yesterday: Snapshot,
    last_git_scan: Option<Instant>,
    last_session_scan: Option<Instant>,
    rx: mpsc::Receiver<Msg>,
    tx: mpsc::Sender<Msg>,
    loading_git: bool,
    loading_sessions: bool,
    last_date_seen: Option<jiff::civil::Date>,
    cal: CalCore,
    meetings_unavailable: bool,
}
```

Update `new`:

```rust
pub fn new() -> Self {
    let (tx, rx) = mpsc::channel();
    Self {
        today: Snapshot::default(),
        yesterday: Snapshot::default(),
        last_git_scan: None,
        last_session_scan: None,
        rx,
        tx,
        loading_git: false,
        loading_sessions: false,
        last_date_seen: None,
        cal: CalCore::new(),
        meetings_unavailable: false,
    }
}
```

Add a helper that pulls meetings from `core.days`:

```rust
fn refresh_meetings(&mut self, now_local: &jiff::Zoned) {
    self.cal.tick();
    let now_ts = now_local.timestamp();
    let today_date = now_local.date();
    let yesterday_date = today_date.checked_sub(1.day()).unwrap_or(today_date);

    let today_events: Vec<_> = self.cal.days.iter()
        .find(|g| g.date == today_date)
        .map(|g| g.events.clone())
        .unwrap_or_default();
    let yesterday_events: Vec<_> = self.cal.days.iter()
        .find(|g| g.date == yesterday_date)
        .map(|g| g.events.clone())
        .unwrap_or_default();

    self.today.meetings = summarize_meetings(&today_events, now_ts);
    self.yesterday.meetings = summarize_meetings(&yesterday_events, now_ts);

    // "unavailable" only when a fetch errored AND we have no calendar data at
    // all. A successful fetch with no events today is "0 meetings", not an error.
    self.meetings_unavailable =
        self.cal.last_fetch_error.is_some() && self.cal.days.is_empty();
}
```

Replace `tick` body to call `refresh_meetings` and handle rollover:

```rust
fn tick(&mut self) {
    while let Ok(msg) = self.rx.try_recv() {
        match msg {
            Msg::Commits { today, yesterday } => {
                self.today.commits = today;
                self.yesterday.commits = yesterday;
                self.loading_git = false;
            }
            Msg::Sessions { today, yesterday } => {
                self.today.sessions = today;
                self.yesterday.sessions = yesterday;
                self.loading_sessions = false;
            }
        }
    }

    let now_local = jiff::Zoned::now();
    let today_date = now_local.date();
    let rolled = match self.last_date_seen {
        Some(d) => d != today_date,
        None => false,
    };
    self.last_date_seen = Some(today_date);

    let stale_git = rolled || self.last_git_scan
        .map(|t| t.elapsed() > Duration::from_secs(300))
        .unwrap_or(true);
    if stale_git && !self.loading_git {
        self.kick_git_scan();
    }

    let stale_sessions = rolled || self.last_session_scan
        .map(|t| t.elapsed() > Duration::from_secs(300))
        .unwrap_or(true);
    if stale_sessions && !self.loading_sessions {
        self.kick_session_scan();
    }

    self.refresh_meetings(&now_local);
}
```

- [ ] **Step 8.2: Build to confirm it compiles**

```
cd ~/projects/glance && cargo build --release
```

Expected: clean build. If `1.day()` doesn't resolve, double-check `use jiff::ToSpan;`.

- [ ] **Step 8.3: Run all tests**

```
cd ~/projects/glance && cargo test --lib panels::standup
```

Expected: all helper tests still pass.

- [ ] **Step 8.4: Manual smoke**

```
./target/release/glance
```

All three rows populate within ~30s.

- [ ] **Step 8.5: Commit**

```
cd ~/projects/glance && git add src/panels/standup.rs src/cal/mod.rs && git commit -m "standup: CalCore-backed meetings + cal-unavailable signal + date rollover"
```

---

### Task 9: Render the scoreboard

Replace placeholder body with the spec layout: title, three rows, optional next-event subline, blank, yesterday line. Magenta when next meeting is within 15 min.

**Files:**
- Modify: `~/projects/glance/src/panels/standup.rs`

- [ ] **Step 9.1: Add formatting helpers**

Above `impl Panel for StandupPanel`:

```rust
fn fmt_hh_mm(secs: i64) -> String {
    let ts = match jiff::Timestamp::from_second(secs) {
        Ok(t) => t,
        Err(_) => return "--:--".to_string(),
    };
    let z = ts.to_zoned(jiff::tz::TimeZone::system());
    let hour12 = match z.hour() % 12 {
        0 => 12,
        n => n,
    };
    let suffix = if z.hour() >= 12 { "PM" } else { "AM" };
    format!("{}:{:02} {}", hour12, z.minute(), suffix)
}

fn fmt_today_header(z: &jiff::Zoned) -> String {
    let weekday = match z.weekday().to_monday_zero_offset() {
        0 => "Mon", 1 => "Tue", 2 => "Wed", 3 => "Thu",
        4 => "Fri", 5 => "Sat", 6 => "Sun", _ => "-",
    };
    let month = match z.month() {
        1 => "Jan", 2 => "Feb", 3 => "Mar", 4 => "Apr",
        5 => "May", 6 => "Jun", 7 => "Jul", 8 => "Aug",
        9 => "Sep", 10 => "Oct", 11 => "Nov", 12 => "Dec",
        _ => "-",
    };
    format!("TODAY · {} {} {}", weekday, month, z.day())
}

fn minutes_until_secs(start_secs: i64, now_secs: i64) -> i64 {
    (start_secs - now_secs) / 60
}
```

- [ ] **Step 9.2: Replace `render`**

```rust
fn render(&self, f: &mut Frame, area: Rect) {
    let now_local = jiff::Zoned::now();
    let now_secs = now_local.timestamp().as_second();

    let header = Line::from(Span::styled(
        format!(" {} ", fmt_today_header(&now_local)),
        theme::pane_header(),
    ));
    let rule = Line::from(Span::styled(
        "─".repeat(area.width.saturating_sub(2).max(0) as usize),
        theme::dim(),
    ));

    let pad = "  ";
    let label_w = 12;

    let count_or_dots = |loading: bool, n: u32| -> String {
        if loading && n == 0 { "  …".to_string() } else { format!("{:>3}", n) }
    };

    // Commits row.
    let commits_count = count_or_dots(self.loading_git, self.today.commits.total);
    let commits_suffix = if self.today.commits.total == 0 && !self.loading_git {
        String::new()
    } else {
        let mut s = format!("across {} repos", self.today.commits.repos_touched);
        if let Some(last) = self.today.commits.last_at {
            s.push_str(&format!(" · last {}", fmt_hh_mm(last)));
        }
        s
    };
    let commits_line = Line::from(vec![
        Span::raw(pad),
        Span::styled(format!("{:label_w$}", "commits", label_w = label_w), theme::historical()),
        Span::styled(commits_count, theme::now()),
        Span::raw("  "),
        Span::styled(commits_suffix, theme::historical()),
    ]);

    // Sessions row.
    let sessions_count = count_or_dots(self.loading_sessions, self.today.sessions.count);
    let sessions_suffix = if self.today.sessions.count == 0 && !self.loading_sessions {
        String::new()
    } else {
        let mut s = "claude code".to_string();
        if let Some(last) = self.today.sessions.last_at {
            s.push_str(&format!(" · last {}", fmt_hh_mm(last)));
        }
        s
    };
    let sessions_line = Line::from(vec![
        Span::raw(pad),
        Span::styled(format!("{:label_w$}", "sessions", label_w = label_w), theme::historical()),
        Span::styled(sessions_count, theme::now()),
        Span::raw("  "),
        Span::styled(sessions_suffix, theme::historical()),
    ]);

    // Meetings row.
    let m = &self.today.meetings;
    let total_meetings = m.done + m.upcoming;
    let (meetings_count, next_summary, suffix_style) = if self.meetings_unavailable {
        ("  —".to_string(), "meetings unavailable".to_string(), theme::dim())
    } else {
        let count = format!("{:>3}", total_meetings);
        let urgent = m
            .next
            .as_ref()
            .is_some_and(|n| (0..=15).contains(&minutes_until_secs(n.start_secs, now_secs)));
        let summary = match &m.next {
            Some(n) => format!("{} done · {} left · next {}", m.done, m.upcoming, fmt_hh_mm(n.start_secs)),
            None if total_meetings > 0 => format!("{} done · 0 left", m.done),
            None => String::new(),
        };
        let style = if urgent { theme::alert() } else { theme::historical() };
        (count, summary, style)
    };
    let meetings_line = Line::from(vec![
        Span::raw(pad),
        Span::styled(format!("{:label_w$}", "meetings", label_w = label_w), theme::historical()),
        Span::styled(meetings_count, theme::now()),
        Span::raw("  "),
        Span::styled(next_summary, suffix_style),
    ]);
    let next_title_line = match (self.meetings_unavailable, &m.next) {
        (false, Some(n)) => Line::from(vec![
            Span::raw(" ".repeat(pad.len() + label_w + 5)),
            Span::styled(format!("\"{}\"", n.title), suffix_style),
        ]),
        _ => Line::from(""),
    };

    // Yesterday line.
    let y = &self.yesterday;
    let yesterday_line = Line::from(vec![
        Span::raw(pad),
        Span::styled(format!("{:label_w$}", "yesterday", label_w = label_w), theme::historical()),
        Span::styled(
            format!(
                "{}c · {}s · {}m",
                y.commits.total, y.sessions.count, y.meetings.done + y.meetings.upcoming,
            ),
            theme::dim(),
        ),
    ]);

    let lines = vec![
        header, rule,
        commits_line, sessions_line, meetings_line, next_title_line,
        Line::from(""),
        yesterday_line,
    ];

    f.render_widget(Paragraph::new(lines), area);
}
```

- [ ] **Step 9.3: Build to confirm it compiles**

```
cd ~/projects/glance && cargo build --release
```

Expected: clean build.

- [ ] **Step 9.4: Run all tests**

```
cd ~/projects/glance && cargo test --lib
```

Expected: full test suite green.

- [ ] **Step 9.5: Manual smoke**

```
./target/release/glance
```

Switch to standup. Compare against spec:
- Header `TODAY · <weekday> <month> <day>`.
- Three rows with right-aligned counts in pink.
- Yesterday line in dim lavender.
- If next meeting is within 15 min, meeting suffix + title go magenta.

- [ ] **Step 9.6: Commit**

```
cd ~/projects/glance && git add src/panels/standup.rs && git commit -m "standup: scoreboard render"
```

---

### Task 10: Register in `suite.toml`

Adds the panel to the `rsuite` manifest so it survives `rsuite apply`.

**Files:**
- Modify: `~/projects/dashboard-suite/suite.toml`

- [ ] **Step 10.1: Inspect the existing panel entries**

```
grep -n '^\[panel\.' ~/projects/dashboard-suite/suite.toml | tail -10
```

Note the format used by sibling entries; copy that shape.

- [ ] **Step 10.2: Add the entry**

Append after the last `[panel.*]` block in `~/projects/dashboard-suite/suite.toml`:

```toml
[panel.standup]
summary = "today scoreboard (commits / sessions / meetings)"
default = true
```

If sibling entries use additional fields (`requires`, `env`), match them. `standup` has no external `requires` and no required env vars.

- [ ] **Step 10.3: Sanity check the manifest**

```
cd ~/projects/dashboard-suite && cargo run --release -- list 2>&1 | grep standup
```

Expected: `standup` appears in the listed panels.

- [ ] **Step 10.4: Commit**

```
cd ~/projects/dashboard-suite && git add suite.toml && git commit -m "rsuite: register standup panel"
```

---

## Verification

End-to-end smoke after all tasks land:

```bash
cd ~/projects/glance && cargo test --release
cd ~/projects/glance && cargo build --release && ./target/release/glance
```

In glance, switch to `standup`. Within ~30 s the three rows populate. Cross-check:

- **Commits:** `git log --since=<today-iso> --until=<now-iso> --format=%cI --all --no-merges` summed across repos under `$WT_ROOTS` (or `~/projects` + `~/Projects`).
- **Sessions:** `find ~/.claude/projects -name '*.jsonl' -newer <today-midnight-marker> | wc -l`.
- **Meetings:** compare against the `cal` panel's today count.

Date rollover: launch glance, leave it open across local midnight, verify next tick re-scans and yesterday picks up what was today.

## Out of scope

- Detail modal or drill-in (this is a tile).
- Tasks/health rows.
- Email/Slack/zele data.
- Configurable thresholds (15-min urgency, refresh cadence).
- Standalone `standup` binary (deferred).

## Self-review notes

- Spec coverage: every spec section maps to a task. Day boundary → Task 2. Three pure helpers → Tasks 3-5. Background threads → Tasks 6-7. Meetings + rollover → Task 8. Render + colors → Task 9. Registry → Task 10.
- Type names consistent: `CommitsSnapshot`, `SessionsSnapshot`, `MeetingsSnapshot`, `EventLite`, `Snapshot`, `Msg`, `StandupPanel`. Field names match between definition and usage.
- No placeholders. Every step has runnable code or commands.
- Signatures consistent across tasks: `summarize_meetings(events: &[Event], now: jiff::Timestamp)`, `count_sessions(mtimes: &[SystemTime], start, end)`, `summarize_commits(lines: &[(String,String,String,String)])`.
